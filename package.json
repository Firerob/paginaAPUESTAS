{
  "name": "airhockey-wager",
  "private": true,
  "version": "0.1.0",
  "description": "Air Hockey 1v1 con apuestas de saldo real. Arquitectura server-authoritative.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w @ah/shared",
    "dev:server": "npm run dev -w @ah/game-server",
    "dev:web": "npm run dev -w @ah/web",
    "dev": "npm run build:shared && concurrently -n server,web -c cyan,magenta \"npm run dev:server\" \"npm run dev:web\"",
    "db:up": "docker compose up -d postgres",
    "db:migrate": "npm run migrate -w @ah/game-server",
    "build": "npm run build:shared && npm run build -w @ah/game-server && npm run build -w @ah/web",
    "start": "npm run start -w @ah/web",
    "start:server": "npm run start -w @ah/game-server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20"
  }
}